/**
 * @fachwerk/schema — JSON-Schemas des Gewerk-Formats (ADR-0004) und daraus
 * abgeleitete TypeScript-Typen. Das Schema ist die Quelle der Wahrheit
 * (Agent-first): Editoren, Engine und Agenten validieren gegen dieselben Dateien.
 *
 * Inhalt entsteht in S-2 (Gewerk-Loader + Validierung).
 */
export const GEWERK_FORMAT_VERSION = 1;
